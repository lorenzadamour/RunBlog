{% include "FOSUserBundle::layout.html.twig" %}
{% include "base.html.twig" %}



<div class="container tableau">
  <h2>Liste d'article</h2>
  <table class="table">
    <thead>
      <tr>
        <th>Titre</th>
        <th>Date</th>
        <th>Publié</th>
        <th>Nombre de jaime</th>
        <th>Actions</th>
      </tr>
    </thead>
    <tbody>
      {% for article in articles %}
          <tr>
              <td><a href="{{ path('article_show', { 'id': article.id }) }}"> <i class="fa fa-eye"></i> {{ article.titre }} </a> </td>
              <td>{{ article.date }}</td>
              <td>{{ article.brouillon }}</td>
              <td>{{ article.nombredeJaime }}</td>
              <td>
                  <ul>
                      <li style="list-style:none;">
                          <a href="{{ path('article_edit', { 'id': article.id }) }}"><i class="fa fa-edit"></i> Editer</a>
                      </li>
                  </ul>
              </td>
          </tr>
      {% endfor %}
    </tbody>
  </table>
  <a href="{{ path('article_new') }}"><button class="btn btn-primary" type="button" name="button"> <i class="fa fa-plus"></i> Nouvel Article</button></a>
</div>
